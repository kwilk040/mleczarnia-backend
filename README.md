# MLECZARNIA BACKEND

## Setup

1. `cp sample.env .env`
2. (Optional) set your env variables in .env
3. Run `docker compose up -d --build`
4. Check `docker logs mleczarnia-backend` to get default admin credentials